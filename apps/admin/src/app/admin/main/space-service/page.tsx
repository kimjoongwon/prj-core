interface UserServicePageProps {
  children: React.ReactNode;
}

const SpaceServicePage = (props: UserServicePageProps) => {
  const { children } = props;
  return <div>SpaceService</div>;
};

export default SpaceServicePage;
