'use client';
import { FormLayout } from '@shared/frontend';
import { observer } from 'mobx-react-lite';

const ServiceEditPage = () => {
  return <FormLayout title={'서비스 생성'}></FormLayout>;
};

export default observer(ServiceEditPage);
