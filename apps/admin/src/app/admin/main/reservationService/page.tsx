'use client';

import { observer } from 'mobx-react-lite';
import React from 'react';

const ReservationServicePage = observer(() => {
  return <div>ReservationService</div>;
});

export default ReservationServicePage;
