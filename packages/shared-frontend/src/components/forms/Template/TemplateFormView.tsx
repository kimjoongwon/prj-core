import { CreateTemplateDto, UpdateTemplateDto } from '../../../model';

interface TemplateFormProps {}

export const TemplateFormView = (props: TemplateFormProps) => {
  return <div>template form</div>;
};
